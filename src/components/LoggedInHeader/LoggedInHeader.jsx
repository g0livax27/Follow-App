export default function LoggedInHeader() {
    return(
        <main>
            <p>FOLLOW Logo</p>
            <p>Greetings, user's name</p>
            <button>Log Out</button>
        </main>
    )
};